class Tags():
    """Class initializer. It has 1 custom parameter, with the special `self` parameter that every method on a class needs as the first parameter.
    """
    def __init__(self, id, label):
        self.id = id
        self.label = label
